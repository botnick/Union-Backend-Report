{
  "name": "electron-dashboard",
  "version": "1.0.0",
  "description": "Control panel for Mico Report Server",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "rimraf dist && electron-packager . \"Mico Dashboard\" --platform=win32 --arch=x64 --out=dist --overwrite --icon=assets/icon.ico --asar",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "electron": "^40.4.1",
    "electron-packager": "^17.1.2",
    "esbuild": "^0.27.3"
  },
  "dependencies": {
    "cross-env": "^10.1.0",
    "dotenv": "^17.3.1"
  }
}